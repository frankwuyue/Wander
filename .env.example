# Used by the Vite dev proxy (server-side only, never sent to browser)
ANTHROPIC_API_KEY=sk-ant-...

# For production, set this as a Cloudflare Worker secret:
#   cd worker && npx wrangler secret put ANTHROPIC_API_KEY
